{
  "license": "MIT",
  "scripts": {
    "build": "yarn build:contract",
    "build:contract": "node ../../../asconfig.js $(pwd)",
    "clean": "rm -rf ./out",
    "test": "echo You must be at the top level to run tests"
  }
}
