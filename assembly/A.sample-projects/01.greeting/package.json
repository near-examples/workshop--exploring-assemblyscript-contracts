{
  "license": "MIT",
  "scripts": {
    "build": "yarn build:contract",
    "build:contract": "node ../../../asconfig.js $(pwd)",
    "clean": "rm -rf ./out",
    "test": "echo You must be at the top level to run most tests",
    "test:simulate:runtime": "yarn build && jest -f simulate.spec"
  },
  "devDependencies": {
    "jest": "^25.5.4",
    "near-sdk-as": "^0.3.0"
  }
}
